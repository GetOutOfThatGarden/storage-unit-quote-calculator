---
phase: 01-calculator-mvp
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [script.js, test.js]
autonomous: true
requirements: [CALC-03]
---

<objective>
Implement the calculation logic and automated verification for the quote calculator.

Purpose: To ensure the calculator performs calculations correctly (15% discount, net total, balance).
Output: script.js with logic, test.js for verification.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-calculator-mvp/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement calculation logic</name>
  <files>script.js</files>
  <behavior>
    - Test 1: (100 sum) * 0.85 = 85 net.
    - Test 2: 85 net - 10 deposit = 75 balance.
  </behavior>
  <action>Implement calculateQuote in script.js to take input values, sum them, apply 15% discount, subtract deposit, and update DOM.</action>
  <verify>
    <automated>node -e "console.log(calculate(100, 10))" # Mocked test check</automated>
  </verify>
  <done>Calculation logic implemented in script.js and connected to DOM events.</done>
</task>

<task type="auto">
  <name>Task 2: Implement verification test</name>
  <files>test.js</files>
  <action>Create a simple test.js that mocks DOM input values and verifies the calculate function output against expected results.</action>
  <verify>
    <automated>node test.js</automated>
  </verify>
  <done>Verification test passes.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - "Calculated values match requirement (15% discount)"
    - "Balance is calculated correctly after deposit"
  </truths>
  <artifacts>
    - path: "script.js"
      provides: "Calculation engine"
    - path: "test.js"
      provides: "Automated verification"
  </artifacts>
  <key_links>
    - from: "script.js"
      to: "index.html"
      via: "event listeners"
  </key_links>
</must_haves>

<verification>
Automated tests pass.
</verification>

<success_criteria>
Calculations match the business rules (Sum * 0.85 - Deposit = Balance).
</success_criteria>

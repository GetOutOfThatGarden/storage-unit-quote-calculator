---
phase: 01-calculator-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, style.css]
autonomous: true
requirements: [CALC-01, CALC-02]
---

<objective>
Create the foundation for the quote calculator with HTML structure and responsive CSS layout.

Purpose: To establish the UI framework upon which the logic will be implemented.
Output: index.html and style.css files.
</objective>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HTML structure</name>
  <files>index.html</files>
  <action>Create index.html with a semantic form for service costs (multiple inputs), a deposit input, and result display areas. Include link to style.css and script.js.</action>
  <verify>
    <automated>test -f index.html</automated>
  </verify>
  <done>index.html exists with required fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement responsive CSS</name>
  <files>style.css</files>
  <action>Create style.css with basic responsive layout (flex/grid) to ensure form fields are usable on different screens.</action>
  <verify>
    <automated>test -f style.css</automated>
  </verify>
  <done>style.css exists and is linked.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - "Calculator form is visible"
    - "Inputs for services and deposit are present"
  </truths>
  <artifacts>
    - path: "index.html"
      provides: "UI structure"
    - path: "style.css"
      provides: "Layout styling"
  </artifacts>
  <key_links>
    - from: "index.html"
      to: "style.css"
      via: "link tag"
  </key_links>
</must_haves>

<verification>
HTML file exists and is linked to CSS.
</verification>

<success_criteria>
Calculator interface is rendered in a browser.
</success_criteria>
